/**
 * Esta variable de enumerables sirve para identificar los tipos de filtros que puede tener la aplicación.
 */
export enum EFilter {
  clear = 0,
  filter_min = 1,
  filter_med = 2,
  filter_max = 3,
}
